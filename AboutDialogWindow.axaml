<Window x:Class="LlamaServerLauncher.AboutDialogWindow"
        xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:LlamaServerLauncher.Resources;assembly=LlamaServerLauncher"
        Title="{Binding AboutTitle}"
         Width="480"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        SizeToContent="Height"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="20" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" HorizontalAlignment="Center">
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Llama Server Launcher" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding InstalledVersionText}" HorizontalAlignment="Center" Opacity="0.8" Margin="0,2,0,0"/>
            <TextBlock Text="{Binding AvailableVersionText}" IsVisible="{Binding ShowAvailableVersion}"
                       HorizontalAlignment="Center" Foreground="{DynamicResource LinkForegroundBrush}" FontSize="11" Margin="0,2,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0}: pytraveler">
                    <Binding Path="Localized.Developer" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
            <TextBlock Text="GitHub: " VerticalAlignment="Center"/>
            <Button Content="pytraveler" Click="OpenGitHubProfile" Cursor="Hand"
                    Foreground="{DynamicResource LinkForegroundBrush}" Background="Transparent" BorderThickness="0"
                    Padding="0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <TextBlock Text="{Binding Localized.AppRepository}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <Button Content="LlamaServerLauncherAvalonia" Click="OpenAppRepository" Cursor="Hand"
                    Foreground="{DynamicResource LinkForegroundBrush}" Background="Transparent" BorderThickness="0"
                    Padding="0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="4" Background="{DynamicResource SeparatorBrush}" Height="1" Margin="0,0,0,10" HorizontalAlignment="Stretch"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="{Binding Localized.ThanksMethelina}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <Button Content="Methelina" Click="OpenMethelinaProfile" Cursor="Hand"
                    Foreground="{DynamicResource LinkForegroundBrush}" Background="Transparent" BorderThickness="0"
                    Padding="0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="6" Background="{DynamicResource SeparatorBrush}" Height="1" Margin="0,0,0,10" HorizontalAlignment="Stretch"/>

        <TextBlock Grid.Row="7" Text="{Binding Localized.OptimizationAttribution}" TextWrapping="Wrap"
                   HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="420" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
            <TextBlock Text="• " VerticalAlignment="Center"/>
            <Button Content="BrunoArsioli/llama-optimus" Click="OpenLlamaOptimusRepo" Cursor="Hand"
                    Foreground="{DynamicResource LinkForegroundBrush}" Background="Transparent" BorderThickness="0"
                    Padding="0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <TextBlock Text="• " VerticalAlignment="Center"/>
            <Button Content="optuna/optuna" Click="OpenOptunaRepo" Cursor="Hand"
                    Foreground="{DynamicResource LinkForegroundBrush}" Background="Transparent" BorderThickness="0"
                    Padding="0" VerticalAlignment="Center"/>
        </StackPanel>

        <Button Grid.Row="10" Content="{Binding Localized.OK}" Click="CloseClick"
                HorizontalAlignment="Center" Padding="30,8" MinWidth="80"/>
    </Grid>
</Window>
